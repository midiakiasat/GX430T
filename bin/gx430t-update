#!/usr/bin/env bash
set -euo pipefail
ROOT="${GX430T_ROOT:-$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)}"
cd "$ROOT"
git pull --ff-only || true
echo "GX430T_UPDATE_DONE=true"
bin/gx430tctl start-bg
