# GX430T Real Sheet Queue v0.3.3

Real operator files verified: CSV, TSV, XLSX, ODS.
Headerless one-column barcode sheets are supported.
Excel/Google Sheets scientific numeric barcode notation is normalized back to plain barcode digits.
Python cgi dependency removed.
Binary-safe HTTP upload uses byte-exact multipart parsing.
HTTP upload verified for XLSX, CSV, TSV, and ODS.
Upload directory is created automatically before saving uploaded files.
